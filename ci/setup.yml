---
.template_setup:
  stage: setup
  tags:
    - docker



setup_cc7_py2_venv:
  extends: .template_setup
  image: ${IPBUS_DOCKER_REGISTRY}/ipbb-dev-cc7:dev-ipbb-imgs__uhal2.7.2
  script:
    - source env.sh
    - ipbb --version
  artifacts:
    when: on_success
    paths:
      - venv/ipbb
      - src/ipbb.egg-info
    expire_in: 1 hour


setup_cc7_py2_sys:
  extends: .template_setup
  image: ${IPBUS_DOCKER_REGISTRY}/ipbb-dev-cc7:dev-ipbb-imgs__uhal2.7.2
  script:
    - sudo yum remove python-ipaddress
    - sudo pip install .
    - ipbb --version


setup_centos8_py3_venv:
  extends: .template_setup
  image: ${IPBUS_DOCKER_REGISTRY}/ipbb-dev-centos8:dev-ipbb-imgs__uhal2.7.2
  script:
    - source env.sh
    - ipbb --version


setup_centos8_py3_sys:
  extends: .template_setup
  image: ${IPBUS_DOCKER_REGISTRY}/ipbb-dev-centos8:dev-ipbb-imgs__uhal2.7.2
  script:
    - sudo python -m pip install .
    - ipbb --version

